#include <signal.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
    struct sigaction sa;

    // sa.sa_flags = 0;  optional
    // sigemptyset(&sa.sa_mask);  optional

    // your code

    return 0;
}